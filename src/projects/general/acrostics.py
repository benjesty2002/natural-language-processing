from data_index.raw import NARROW_WORD_LIST
from data_index.opted import OPTED_VALID


input_words = [["access", "inside", "go", "open"], ["room", "enter"], ["unlock", "step"]]

for word in OPTED_VALID.load():
    if len()